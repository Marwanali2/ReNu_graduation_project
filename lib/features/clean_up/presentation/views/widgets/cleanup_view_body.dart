import 'package:flutter/material.dart';

class CleanUpViewBody extends StatelessWidget {
  const CleanUpViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CleanUpViewBody'),
    );
  }
}
