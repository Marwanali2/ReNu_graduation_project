import 'package:flutter/material.dart';

class CleanupCheckView extends StatelessWidget {
  const CleanupCheckView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
