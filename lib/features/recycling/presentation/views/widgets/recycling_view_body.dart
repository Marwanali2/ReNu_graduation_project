import 'package:flutter/material.dart';

class RecyclingViewBody extends StatelessWidget {
  const RecyclingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}